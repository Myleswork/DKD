# env 

- conda create -n env_name python=3.8.20
- conda actiavte env_name

- install pytorch(pip install torch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 --index-url https://download.pytorch.org/whl/cu124)

- pip install -r requirements.txt

# run
- download the teacher_ckpt from github release & place it on root
- dataset: mkdir data & place your dataset inside
- example: